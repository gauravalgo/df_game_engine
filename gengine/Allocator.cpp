#include "Allocator.h"
/*using namespace Defragmentation;
void* Allocator::Allocator(uint unit_size, uint block_size =16)
{
    _block_size=block_size;
    _requested_size=unit_size;
    _allocated_size=max(unit_size,sizeof(free));
    _num_allocated_blocks=
}*/
